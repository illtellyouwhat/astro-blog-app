# LOCAL_SETUP_AND_TEST

## Environment Baseline
- Node.js 20.x with npm as the package manager.
- Repositories cloned under `doug-web/` to keep relative paths stable for scripts.

## TODO
- [ ] Write step-by-step local install, dev server, and preview commands.
- [ ] Include instructions for adding sample Markdown/MDX posts during QA.
- [ ] Capture validation checks (lint, build, link checker).
